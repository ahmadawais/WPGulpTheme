"use strict";

/**
 * Custom JavaScript
 *
 * @since 1.0.0
 */
var sum = function sum(a, b) {
  return a + b;
};
"use strict";

/**
 * Custom JavaScript
 *
 * @since 1.0.0
 */
var sub = function sub(a, b) {
  return a - b;
};