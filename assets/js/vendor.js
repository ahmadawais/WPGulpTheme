"use strict";

/**
 * Vendor JavaScript
 *
 * @since 1.0.0
 */
var divide = function divide(a, b) {
  return a / b;
};
"use strict";

/**
 * Vendor JavaScript
 *
 * @since 1.0.0
 */
var multiply = function multiply(a, b) {
  return a * b;
};